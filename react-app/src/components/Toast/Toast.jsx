// The ToastContext already renders toast notifications, confirm dialogs,
// and prompt dialogs inline within its provider.
// This component serves as a named export for consistency.
export default function Toast() {
  return null;
}
